#!/usr/bin/env bash
# START_HERE.command  —  macOS launcher
# Double-click this file in Finder to start the Job Application Assistant.
# If macOS says "cannot be opened because the developer cannot be verified",
# right-click → Open → Open.

set -euo pipefail
cd "$(dirname "$0")"
bash setup-for-beginners.sh
